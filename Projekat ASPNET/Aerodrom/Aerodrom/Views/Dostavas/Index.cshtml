@model IEnumerable<Aerodrom.Models.Dostava>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kupac)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kupac)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DostavaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DostavaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DostavaId })
        </td>
    </tr>
}

</table>
