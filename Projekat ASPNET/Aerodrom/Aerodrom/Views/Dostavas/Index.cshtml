@model IEnumerable<Aerodrom.Models.Dostava>

@{
    ViewBag.Title = "Dostave za obaviti";
}

<h2>Dostave za obaviti</h2>

<p>
   
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kupac)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kupac)
        </td>
        <td>
            @Html.ActionLink("Gotovo", "Delete", new { id=item.DostavaId })
        </td>
    </tr>
}

</table>
